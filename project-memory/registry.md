# Project Registry

Registered repositories for the Development Orchestrator. Register only verified repositories.
Never store credentials or full remote URLs containing tokens.

| Project key | Repository | Local root | Sanitized remote | Default branch | Stack | Registered | Last accessed |
| ----------- | ---------- | ---------- | ---------------- | --------------- | ----- | ---------- | -------------- |
| _none yet_ | | | | | | | |

Details: `projects/<PROJECT-KEY>/project-profile.md` (created by the `project-bootstrap` skill
the first time the orchestrator runs against a new repository).
